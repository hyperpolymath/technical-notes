[tools]
# technical-notes is a documentation archive: no source code, no build,
# no test suite. git is the only tool required to work on it; typst is
# needed only to reproduce a note's rendered PDF from its .typ source.
git = "latest"
typst = "latest"
